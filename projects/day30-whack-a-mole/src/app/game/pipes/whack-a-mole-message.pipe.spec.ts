import { WhackAMoleMessagePipe } from './whack-a-mole-message.pipe';

describe('WhackAMoleMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new WhackAMoleMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
