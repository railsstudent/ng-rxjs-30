import { ElementRef } from '@angular/core';
import { HighlightAnchorDirective } from './highlight-anchor.directive';

describe('HighlightAnchorDirective', () => {
  it('should create an instance', () => {
    // const directive = new HighlightAnchorDirective(new ElementRef<HTMLAnchorElement>(''));
    // expect(directive).toBeTruthy();
  });
});
