import { HighlightSuggestionPipe } from './highlight-suggestion.pipe';

describe('HighlightSuggestionPipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightSuggestionPipe();
    expect(pipe).toBeTruthy();
  });
});
